# https://lefthook.dev/configuration/
pre-commit:
  commands:
    # Formatting is explicit: hooks must not rewrite or restage unrelated work.
    check:
      run: mise run check

pre-push:
  commands:
    test:
      run: mise run test
