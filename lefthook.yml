# https://lefthook.dev/configuration/
pre-commit:
  parallel: false
  commands:
    format:
      run: mise run format
      stage_fixed: true
    check:
      run: mise run check

pre-push:
  commands:
    test:
      run: mise run test
